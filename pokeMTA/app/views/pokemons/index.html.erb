<h1> All The Pokemon! (who have been caught and are with Trainers) </h1>

<ul> 
    <% @pokemon.each do |poke| %>
    <li> 
    <a href = "<%= pokemon_url(poke) %> ">
    <%= poke.name %>
    </li>
    <% end %>
</ul>

